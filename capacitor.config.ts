import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.saisandeep.costcosaverpro',
  appName: 'Costco Saver Pro',
  webDir: 'dist'
};

export default config;
